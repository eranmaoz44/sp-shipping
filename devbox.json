{
  "packages": [
    "nodejs@22",
    "docker",
    "docker-compose"
  ],
  "shell": {
    "scripts": {
      "setup": "npm --prefix apps/api install && npm --prefix apps/frontend install",
      "dev": "sh -c \"npm --prefix apps/api run dev & npm --prefix apps/frontend run dev\"",
      "docker:up": "docker compose up --build",
      "docker:down": "docker compose down"
    }
  }
}
